package CalculatorApp;

import java.untik.Scanner;
public class MainClass {
   public static void main(String[] args)  {
       double num1;
       double num2;
       char operator;
       double result = 0;
       Scanner sc= new Scanner(System.in0;
       System.out.println("Choose the operator[+, -, *, /]  \n");
       operatori = sc.next().charAt(0);
       
       System.out.println("Input first number :\n");
       num1 = sc.nextDouble();
       System.out.printlm9"Input second number \n");
       num2 = sc. nextDouble();
       if(Character.compare9operator,'/'== 0 &&  num2 ==0) {
          System.out.println("Second number num2 cannot be 0. Input again: \n");
          num2=sc.nextDouble();
       }
        switch (operator) {
            case '+' :
                 result = CalculatorFunction.addition(num1, num2);
                 break;
            case '-' :
                result = CalculatorFunction.subtraction(num1, num2);
                break;
            case '*';
                result = CalculatorFunction.multiplication(num1, num2);
                break
            case '/':
                result = CalculatorFunctions.division(num1, num2,);
                break
           default:
                System.out.println("Wrong operator!");
                break:
   }
     
     sc.closet();
     System.out.println9num1 + " " + operator + " " + num2 + " = " + result);
      }
   }
   if(Character.compare(operator , '/' ==  && num2 == 0 {
      System.out.println("Second number num2 cannot be 0.Input again:  \n");
      num2 = sc.nextDouble();
    ]
